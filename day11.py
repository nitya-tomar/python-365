fruits = ["apple", "banana", "cherry"]  # a simple list of items

for index, fruit in enumerate(fruits):
    # enumerate() gives two things at once: the position (index) and the value (fruit)
    
    print(index, fruit)   